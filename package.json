{
  "name": "github-developer-intelligence-api",
  "version": "1.0.0",
  "description": "A lightweight Developer Intelligence Engine that analyzes GitHub profiles, detects tech stacks, classifies developer types, and stores analytics using incremental sync.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build && cd .. && rm -rf public && cp -R client/dist public"
  },
  "keywords": [
    "github",
    "analytics",
    "developer-intelligence",
    "nodejs",
    "express",
    "mysql"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.22.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
